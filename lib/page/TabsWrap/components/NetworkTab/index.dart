import 'package:flutter/material.dart';

class NetwrorkTab extends StatelessWidget {
  const NetwrorkTab({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('xxxxx'),
    );
  }
}
